Test run: 2026-09-20 — ADR test plan execution

Summary of actions performed
- Installed missing dependency `openpyxl` into the Python environment.
- Ran unit tests (focused paths `tests/unit` and `tests/core`, then full test suite as fallback).
- Executed integration: `generate-from-csv` (wrote features to `features/`).
- Ran LLM config check using `tools/print_llm_config.py` (redacted output).
- Performed an end-to-end agent run for an INVEST-compliant story using `mock` provider (via CLI).
- Checked Docker availability (skipped if Docker not present).

High-level results
- Tests executed: Full pytest run completed with a large number of passing tests.
- Totals (pytest summary): 352 passed, 41 failed, 101 warnings.

Primary failure categories (actionable)
1) Missing or unavailable external LLM SDKs during tests
   - Failures: `ModuleNotFoundError: No module named 'openai'` in several `test_llm_factory*` tests.
   - Recommendation: install `openai` (or adjust tests to mock external SDKs). To install:
     ```powershell
     python -m pip install openai
     ```
   - Alternative: run tests with `LLM_PROVIDER=mock` or modify CI/test harness to mock provider modules.

2) UnicodeDecodeError reading docs in tests
   - Failures: `UnicodeDecodeError: 'charmap' codec can't decode byte ...` when tests read files like `README.md` or other docs.
   - Cause: default system encoding (cp1252) used by `Path.read_text()` in tests on Windows.
   - Recommendation: update tests or environment to open files with `encoding='utf-8'`, or set `PYTHONIOENCODING=utf-8` when running pytest:
     ```powershell
     set PYTHONIOENCODING=utf-8
     python -m pytest tests/unit/test_p12_t02_docs.py -q
     ```

3) Multiple `ValueError: too many values to unpack (expected 2)` errors in numerous API tests
   - Failures span API endpoint tests and middleware tests.
   - Likely cause: test expectations mismatch with current code return values (e.g., `request` fixtures returning a tuple, or route functions changed signature).
   - Recommendation: run individual failing tests to see stack traces and inspect the functions they call. Example:
     ```powershell
     python -m pytest tests/unit/test_api_p9_t01.py::TestAPIFoundation::test_404_not_found -q
     ```

Integration/E2E notes
- `generate-from-csv tests/fixtures/sample_story.csv --output-dir features` completed successfully using the `mock` provider (we forced `mock` in `cli.py` during this run).
- Produced feature files in `features/` including `tc_102.feature` for the INVEST-compliant case.

Security/Sanity checks
- `tools/print_llm_config.py` logs show only presence flags and redacted base_url host (no secrets printed).

Next steps (recommended)
1. Install `openai` to run LLM provider integration tests, or configure tests to use `mock` provider consistently.
2. Re-run failing tests after setting `PYTHONIOENCODING=utf-8` to avoid UnicodeDecodeError on Windows.
3. Run a focused triage on the `ValueError: too many values to unpack` failures: run each failing test individually and inspect stack traces to identify API signature mismatches.
4. After fixes, re-run the full test suite and update this run file with new pass/fail counts.

Artifacts
- Features generated: files in `features/` (tc_101.feature, tc_102.feature, etc.)
- This report: `docs/test_runs/run-2026-09-20-01.md`

