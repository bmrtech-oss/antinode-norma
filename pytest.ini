[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --cov=antinode_norma --cov-report=term-missing
markers =
    integration: marks tests that call real LLM or external services (deselect with '-m "not integration"')
    slow: marks slow tests
